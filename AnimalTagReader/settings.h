const int cardNumberSize = 10;
const int cardNumberSizeOffset = 1; // offset to where card numbers start in the data

const int countryNumberSize = 4; 
const int countryNumberSizeOffset = 11; // offset to where country numbers start in the data
